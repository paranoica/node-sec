//! `api` — read-only dashboard APIs over the compliance/case layer (D-022).
//!
//! T055 delivers the analyst review-queue API; the simulation dashboard API follows in S6.
#![forbid(unsafe_code)]

pub mod analyst;
