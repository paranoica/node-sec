apiVersion: v1
kind: Pod
metadata:
  name: worker
spec:
  containers:
    - name: worker
      image: app:1.0
      securityContext:
        privileged: true          # host namespaces + all caps -> trivial node escape
